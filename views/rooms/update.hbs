  <h3>Edit room</h3>
<form method="POST" action="/rooms/{{ room._id}}" enctype="multipart/form-data">
    <label>Name:</label>
    <input type="" name="name" placeholder="Name" value="{{ room.name}}">
    <label>Description:</label>
    <input type="" name="description" placeholder="Description" value="{{ room.description}}">
    
    <img src="{{room.imageUrl}}" alt="">
    
    <input type="file" name="photo">

    {{!-- <label for="type" value="{{place.type}}">Place Type</label><br> --}}
    {{!-- <option value="bookstore" {{selected place.type 'bookstore'}}>bookstore</option> --}}
    {{!-- <select name='type'>
        <option value="Coffee shop" {{selected place.type 'Coffee shop'}}>Coffee shop</option>
        <option value="Book store" {{selected place.type 'Book store'}}>Book store</option>
    </select> --}}
    <label>Address</label>
        <input type="text" name="latitude" id="latitude" placeholder="Latitude" value="{{ place.location.coordinates.[1]}}">
        <input type="text" name="longitude" id="longitude" placeholder="Longitude" value="{{ place.location.coordinates.[0]}}">
    <button type="submit" name="" id="submit" >Edit</button>
</form>
